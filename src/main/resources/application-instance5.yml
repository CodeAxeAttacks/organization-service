server:
  port: 8085

spring:
  cloud:
    consul:
      discovery:
        instance-id: ${spring.application.name}:instance3:${server.port}
        tags:
          - instance=5
          - version=1.0.0
